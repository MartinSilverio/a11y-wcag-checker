import React from 'react';
import './ListSectionTitle.scss';

function ListSectionTitle({ title }: any) {
  return <h2 className='list-section-title'>{title}</h2>;
}

export default ListSectionTitle;
